{
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ],
  "routes": [
    {
      "source": "/api/:path*",
      "methods": ["GET", "POST", "PUT", "PATCH", "DELETE"],
      "dest": "proxy"
    }
  ]
}
